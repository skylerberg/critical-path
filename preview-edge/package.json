{
  "name": "critical-path-preview-edge",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --format=esm --outfile=dist/index.mjs --packages=external",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.21.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "esbuild": "^0.28.1",
    "typescript": "6.0.3"
  }
}
